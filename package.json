{
    "name": "project-root",
    "version": "0.1.0a",
    "private": false,
    "type": "commonjs",
    "scripts": {
        "tree-client": "mkdir -p ./scripts/generated/generate-folder-tree && node ./scripts/generate-folder-tree.js ./client ./scripts/generated/generate-folder-tree/client-tree.md --depth 5",
        "tree-server": "mkdir -p ./scripts/generated/generate-folder-tree && node ./scripts/generate-folder-tree.js ./server ./scripts/generated/generate-folder-tree/server-tree.md --depth 3",
        "tree-root": "mkdir -p ./scripts/generated/generate-folder-tree && node ./scripts/generate-folder-tree.js ./ ./scripts/generated/generate-folder-tree/root-tree.md --depth 3"
    },
    "devDependencies": {
        "eslint": "^9.36.0"
    }
}
